import Flux from '../../../local/index';

@Flux.View.Component({
  selector: 'CustomController',
  template: (`<div></div>`)
})
class CustomController extends Flux.ControllerView {
}

export default CustomController;
