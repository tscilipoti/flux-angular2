import Flux from '../../../local/index';

@Flux.View.component({
  selector: 'DetailsView',
  template: (`<div>
    <span>Details</span>
  </div>`)
})
class DetailView {
}

export default DetailView;
